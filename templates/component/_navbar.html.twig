<nav class="navbar" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <a href="{{ path('tricks_index') }}" class="navbar-item has-text-weight-bold is-italic is-size-2">
            SnowTricks
        </a>

        <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample" id="burger">
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
        </a>
    </div>
    <div id="navbarBasicExample" class="navbar-menu">
        <div class="navbar-start">
            <a class="navbar-item" href="{{ path('tricks_index') }}">Tricks</a>
        </div>
        {% if app.user is null %}
            <div class="navbar-end">
                <div class="navbar-item">
                    <div class="buttons">
                        <a href="{{ path('app_register') }}" class="button is-primary">
                            <strong>Inscription</strong>
                        </a>
                        <a href="{{ path('app_login') }}" class="button is-light">
                            Log in
                        </a>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="navbar-item has-dropdown is-hoverable mr-3">
                <a class="navbar-link">
                    {{ app.user.userName }}
                </a>
                <div class="navbar-dropdown is-right">
                    <a href="{{ path('app_dashboard') }}" class="navbar-item">Dashboard</a>
                    <a href="{{ path('tricks_new') }}" class="navbar-item">Nouveau Trick</a>

                    <hr class="navbar-divider">
                    <a href="{{ path('app_logout') }}" class="navbar-item">Deconnexion</a>
                </div>
            </div>
        {% endif %}
    </div>
</nav>



